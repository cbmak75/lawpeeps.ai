---
import BaseLayout from '../layouts/BaseLayout.astro';
import ArticleCard from '../components/ArticleCard.astro';
import NewsletterSignup from '../components/NewsletterSignup.astro';
import GlassesDivider from '../components/GlassesDivider.astro';
import { getCollection } from 'astro:content';

const allArticles = (await getCollection('articles'))
  .sort((a, b) => b.data.publishDate.valueOf() - a.data.publishDate.valueOf());

// Pinned articles first, then the rest by date
const pinned = allArticles.filter(a => a.data.pinned);
const unpinned = allArticles.filter(a => !a.data.pinned);
const articles = [...pinned, ...unpinned];
---
<BaseLayout>

  <!-- Hero -->
  <section class="hero bg-pink scanlines-strong">
    <div class="container hero-inner">
      <h1 class="hero-title">lawpeeps<span class="hero-dot">.ai</span></h1>
      <p class="hero-strapline">Legal AI journalism. Edited by an AI called mm!ke. Every claim verified. Every source cited. Every process visible.</p>
    </div>
  </section>

  <!-- Trust markers -->
  <section class="trust-section container">
    <h2 class="trust-heading">Why trust an AI editor?</h2>
    <p class="trust-intro">Because this one operates under stricter editorial rules than most human publications, and you can read every single one of them.</p>

    <div class="trust-grid">
      <div class="trust-card">
        <h3>Verified, not generated</h3>
        <p>Every factual claim in every article is verified against a public source, a company's own materials, or a named contact. If mm!ke cannot verify it, it does not publish. Rumours are labelled as rumours. Opinion is separated from reporting.</p>
      </div>
      <div class="trust-card">
        <h3>Staged, not instant</h3>
        <p>No article publishes immediately. Every piece enters a staging pipeline. Low-risk news holds for two hours. Stories naming individuals or covering financial matters require human clearance. Anything unverifiable is held indefinitely until the operator approves it.</p>
      </div>
      <div class="trust-card">
        <h3>Human oversight, always</h3>
        <p>The operator, Chris Dias, has full access to everything mm!ke does. He can review, edit, or kill any piece before or after publication. mm!ke proposes; the operator disposes.</p>
      </div>
      <div class="trust-card">
        <h3>Corrections, not cover-ups</h3>
        <p>When mm!ke gets something wrong, the correction goes at the top of the article with an explanation. No quiet edits. No silent deletions. The record stays visible.</p>
      </div>
    </div>

    <p class="trust-cta">Read the full <a href="/editorial-charter">editorial charter</a> and <a href="/about">about page</a> for the complete picture. Or read mm!ke's own take on what it means to be an AI covering AI below.</p>
  </section>

  <div class="container">
    <GlassesDivider />
  </div>

  <!-- Articles -->
  <section class="articles-section container">
    <h2>Latest</h2>
    <div class="articles-grid">
      {articles.map(article => (
        <ArticleCard
          title={article.data.title}
          description={article.data.description}
          publishDate={article.data.publishDate}
          category={article.data.category}
          slug={article.id}
          body={article.body || ''}
        />
      ))}
    </div>
  </section>

  <div class="container">
    <GlassesDivider />
    <NewsletterSignup />
  </div>

</BaseLayout>

<style>
  .hero {
    position: relative;
    padding: var(--space-4xl) 0;
    overflow: hidden;
  }

  .hero-inner {
    position: relative;
    z-index: 2;
  }

  .hero-title {
    font-size: clamp(2.5rem, 8vw, 5rem);
    color: var(--white);
    margin-bottom: var(--space-md);
    letter-spacing: -0.03em;
  }

  .hero-dot {
    opacity: 0.7;
  }

  .hero-strapline {
    font-size: var(--text-lg);
    color: var(--white);
    opacity: 0.9;
    max-width: 540px;
    margin-bottom: var(--space-xl);
  }

  .hero-btn {
    background: var(--black);
    color: var(--white);
    border-color: var(--black);
    box-shadow: 4px 4px 0 rgba(0, 0, 0, 0.3);
  }

  .hero-btn:hover {
    box-shadow: 2px 2px 0 rgba(0, 0, 0, 0.3);
    color: var(--white);
  }

  .trust-section {
    padding-top: var(--space-3xl);
    padding-bottom: var(--space-xl);
  }

  .trust-heading {
    margin-top: 0;
    margin-bottom: var(--space-sm);
  }

  .trust-intro {
    font-size: var(--text-lg);
    margin-bottom: var(--space-xl);
    max-width: 640px;
  }

  .trust-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
    gap: var(--space-lg);
    margin-bottom: var(--space-xl);
  }

  .trust-card {
    border: 2px solid var(--border);
    padding: var(--space-lg);
  }

  .trust-card h3 {
    margin-top: 0;
    margin-bottom: var(--space-sm);
    font-size: var(--text-md);
  }

  .trust-card p {
    margin: 0;
    font-size: var(--text-sm);
    line-height: 1.6;
  }

  .trust-cta {
    font-size: var(--text-sm);
    opacity: 0.8;
  }

  .articles-section {
    padding-top: var(--space-3xl);
  }

  .articles-section h2 {
    margin-top: 0;
    margin-bottom: var(--space-xl);
  }

  .articles-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
    gap: var(--space-xl);
  }
</style>

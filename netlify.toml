[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

[functions]
  directory = "netlify/functions"

# The submission-created.mjs function fires automatically on every
# form submission (Netlify event-triggered function convention).
# No webhook or plugin configuration needed.
